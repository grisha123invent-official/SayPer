# SayPer

[English](README.md) · **Русский**

Зажал клавишу, сказал, отпустил — текст появился там, где стоит курсор.

Диктовка для macOS, живёт в строке меню. Записывает голос, отправляет в OpenAI Whisper и вставляет расшифровку в то приложение, где ты печатал. Окна нет, в Dock не висит.

<img src="docs/pill.jpg" width="480" alt="Индикатор записи">

## Что нужно

- **macOS 26 или новее.** Собрано под SDK macOS 26 и использует то, чего в прежних версиях нет.
- **Apple Silicon.** Сборка только arm64, на Intel не пойдёт.
- **Свой ключ OpenAI.** Расшифровка стоит около $0.006 за минуту речи — примерно 36 центов за час диктовки. Платишь напрямую OpenAI.

## Установка

Скачай из [релизов](https://github.com/grisha123invent-official/SayPer/releases). Годится любой файл: `.dmg` красивее, `.zip` легче и блокируется на один раз меньше.

Приложение подписано, но **не заверено Apple** — для заверения нужно платное членство в Developer Program. Поэтому macOS вешает на скачанный файл карантин и открывать его отказывается. Обойти это можно двумя путями.

### Одна команда, и дальше всё само

1. Открой **Терминал** (⌘Пробел, набери «Терминал»).
2. Набери вот это — **с пробелом в конце** — и пока не жми Enter:

   ```
   xattr -d com.apple.quarantine 
   ```

3. **Перетащи скачанный файл из Finder прямо в окно Терминала.** Полный путь подставится сам — неважно, где файл лежит и как называется. В этом весь смысл перетаскивания: если скачал дважды, вторая копия называется `SayPer-1.0-beta.1 2.dmg`, и набирать такое руками — верный способ ошибиться.
4. Нажми **Enter**.

Теперь открывай образ, перетаскивай SayPer в «Программы» и запускай — ни одного предупреждения. Снятый с образа карантин снимается и с приложения внутри, окно безопасности не появится вообще.

С архивом так же: сначала снять карантин, потом распаковать. Можно и наоборот — тогда команду нужно направить на само приложение.

**Уже перетащил в «Программы» и получил блокировку?** Снимай карантин с приложения. Ключ `-r` нужен потому, что приложение — это папка:

```bash
xattr -dr com.apple.quarantine /Applications/SayPer.app
```

**В ответ пришло «No such xattr»?** Значит карантина на файле и не было — просто открывай.

### Без Терминала

1. Открой скачанный файл. macOS скажет, что не смогла его проверить, — нажми **Done**, а не «Move to Trash».
2. Зайди в **Системные настройки → Конфиденциальность и безопасность**, пролистай вниз. Там будет строка про заблокированный файл и кнопка **«Всё равно открыть»**. Нажми и подтверди.
3. Образ откроется. **Перетащи SayPer в «Программы»** — не запускай из образа или из «Загрузок»: оттуда macOS запускает копию из случайной папки только для чтения, и выданные разрешения не сохранятся.
4. Запусти приложение. **Оно заблокируется второй раз** — карантин переходит с образа на приложение. Повтори шаг 2 уже для него.

С архивом один круг отпадает: распаковал, перетащил в «Программы» — блокировка будет только одна, на самом приложении.

Правый клик → «Открыть» больше не работает: Apple убрала этот обход в свежих версиях macOS.

<img src="docs/install.jpg" width="560" alt="Окно установки">

## Первый запуск

Мастер проведёт по всему, что нужно:

- **Микрофон** — без него записывать нечего.
- **Универсальный доступ** и **Мониторинг ввода** — два разных разрешения, нужны оба. Первое позволяет вставлять текст в чужие окна, второе — видеть горячую клавишу, пока работаешь в другой программе. Выдать их себе приложение не может, переключатели живут в системных настройках. Мастер замечает их сам, как только включишь: ни перезапуска, ни кнопки «я включил».
- **Ключ OpenAI** — создаётся на [platform.openai.com](https://platform.openai.com/api-keys). Показывается один раз, копируй сразу.

## Как пользоваться

Зажми сочетание, говори, отпусти. Текст встанет туда, где курсор.

- **Esc** прерывает что угодно: и запись, и ожидание ответа.
- Держать клавишу неудобно — включи режим **«нажал-нажал»** в разделе «Диктовка». Забытая запись остановится сама через пять минут, и текст ляжет в буфер, а не в случайное поле.
- Сочетание любое, включая одиночные модификаторы. Левые и правые клавиши различаются.
- Если хоткей состоит только из модификаторов, обычная клавиша во время записи её отменяет — так привычные шорткаты продолжают работать.

## Что ещё умеет

<img src="docs/settings.jpg" width="560" alt="Настройки">

- **История** расшифровок, хранится локально. Любую можно скопировать или вставить заново.
- **Расходы** — оценка потраченного у OpenAI по дням и месяцам.
- **Приглушение звука** — музыка и видео лезут в микрофон и портят расшифровку, поэтому на время записи звук компьютера убавляется или выключается, а потом возвращается.
- **Словарь** — имена и термины, которые модель начинает узнавать.
- **Причёсывание** — необязательный второй проход: пунктуация и слова-паразиты.
- **Оформление** — шесть акцентных цветов, светлая/тёмная/системная тема, звуки по событиям с ползунком громкости.

## Приватность

- Звук уходит в **OpenAI** и больше никуда. Отправляется только на время расшифровки, на диске не остаётся.
- Ключ лежит на твоём маке — в связке ключей, а если она недоступна локально подписанному приложению, то в файле с правами `0600` в Application Support.
- История и расходы хранятся локально, в `~/Library/Application Support/SayPer`.
- Никакой телеметрии, аналитики, отчётов о сбоях и наших серверов.

Если отправлять звук в OpenAI неприемлемо — `Transcriber` это один файл, вместо него ставится локальный `whisper.cpp`.

## Обновления

Автообновления пока нет. Новые версии выходят релизами здесь — скачиваешь и заменяешь.

Разрешения переживают обновление: все сборки подписаны одним сертификатом.

## Сборка из исходников

Нужны только Command Line Tools, Xcode не требуется.

```bash
./build.sh          # собрать в dist/
./build.sh install  # собрать, поставить в /Applications и запустить
./build.sh dmg      # собрать образ
```

Устройство, подпись и разбор проблем — в [docs/development.md](docs/development.md).

## Забирай и переделывай

Приложение здесь целиком — 52 файла Swift, без зависимостей и пакетных менеджеров. Клонируешь, запускаешь `./build.sh install`, и через минуту у тебя своя копия. Меняй поведение хоткея, ставь вместо Whisper локальную модель, перекраивай окно — это твоё дело.

Пул-реквесты и замечания приветствуются. Если лезешь в интерфейс — в [design/](design/) описаны токены и состояния компонентов, из которых он собран.

## Состояние

Бета. Работает и используется каждый день, шероховатости ожидаемы.

## Лицензия

[MIT](LICENSE) — делай что хочешь, только сохрани строку об авторстве.
