import SwiftUI

/// Раздел «История»: последние расшифровки, копирование и повторная вставка.
/// Наполняет слайс «История» — здесь только место под него.
struct SettingsSectionHistory: View {
    var body: some View {
        SectionPlaceholder(symbol: SettingsSection.history.symbol)
    }
}
