import SwiftUI

/// Раздел «Расходы»: минуты, слова и оценка стоимости.
/// Наполняет слайс «Расходы» — здесь только место под него.
struct SettingsSectionUsage: View {
    var body: some View {
        SectionPlaceholder(symbol: SettingsSection.usage.symbol)
    }
}
